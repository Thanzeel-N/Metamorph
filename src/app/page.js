import HomeClient from "./page-client";

export const metadata = {
  title: "Metamorph.live — Premium Business Solutions",
  description: "We build brands, run marketing, produce media, and ship IT systems — all under one roof.",
};

export default function Page() {
  return <HomeClient />;
}
